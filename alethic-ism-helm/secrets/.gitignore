# Ignore all actual API keys and credentials (but allow .sample files)
*.api-key
*-key.json
*.credentials.json
firebase-credentials.json

# Explicitly allow sample files
!*.sample
!*.api-key.sample
!*.json.sample
